{
  "name": "utshorgo",
  "private": true,
  "version": "1.0.0",
  "description": "Utshorgo — the reseller-first marketplace (React + TS + Node + Supabase)",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "dev:server": "npm run dev -w server",
    "dev:client": "npm workspaces run dev 2>/dev/null || npm run dev -w client",
    "build": "npm run build -w client",
    "start": "npm run start -w server",
    "typecheck": "npm run typecheck -w client && npm run typecheck -w server",
    "seed": "npm run seed -w server"
  },
 "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
