# Copy to server/.env and fill in to enable live Supabase data.
# Without these, the server runs in demo mode with seed data.
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Optional: override the API port (defaults to 4000)
# PORT=4000
